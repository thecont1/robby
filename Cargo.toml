[package]
name = "robby"
version = "0.1.0"
edition = "2021"
description = "A tiny explainable compiler for visual composition scripts"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

